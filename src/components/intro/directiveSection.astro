---
import { directiveExamples } from "@/constants/intro";
---

<section class="py-20">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-5xl font-bold text-white mb-6">
        Directrices de Hidratación
      </h2>
      <p class="text-xl text-blue-200 max-w-4xl mx-auto">
        Por defecto, Astro renderizará automáticamente cada componente de UI a
        solo HTML y CSS, eliminando todo el JavaScript del lado del cliente
        automáticamente. Convertir cualquier componente de UI estático en una
        isla interactiva solo requiere una directiva client:*. Astro luego
        construye y empaqueta automáticamente tu JavaScript del lado del cliente
        para un rendimiento optimizado.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-8">
      {
        directiveExamples.map((directive) => (
          <div class="bg-white/5 backdrop-blur-lg rounded-2xl p-8 border border-white/10 hover:border-white/20 transition-all duration-300 group">
            <div class="flex items-center justify-between mb-6">
              <code class="text-lg font-mono bg-blue-600/30 text-blue-200 px-4 py-2 rounded-lg">
                {directive.directive}
              </code>
              <span
                class={`px-3 py-1 rounded-full text-sm font-semibold ${
                  directive.priority === "Crítica"
                    ? "bg-red-600/30 text-red-200"
                    : directive.priority === "Media"
                      ? "bg-yellow-600/30 text-yellow-200"
                      : directive.priority === "Bajo"
                        ? "bg-green-600/30 text-green-200"
                        : "bg-purple-600/30 text-purple-200"
                }`}
              >
                {directive.priority}
              </span>
            </div>

            <p class="text-white font-semibold mb-6">{directive.description}</p>

            <div class="space-y-3 mb-6">
              <div class="flex justify-between items-center">
                <span class="text-slate-300 text-sm">Bundle:</span>
                <span class="text-blue-200 text-sm font-medium">
                  {directive.jsBundle}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-slate-300 text-sm">Performance:</span>
                <span class="text-blue-200 text-sm font-medium">
                  {directive.performance}
                </span>
              </div>
              <div class="flex justify-between items-start">
                <span class="text-slate-300 text-sm">Caso de uso:</span>
                <span class="text-blue-200 text-sm font-medium text-right flex-1 ml-4">
                  {directive.useCase}
                </span>
              </div>
            </div>

            <div class="bg-slate-900/50 rounded-xl p-4 border border-slate-700 group-hover:border-slate-600 transition-colors">
              <pre class="text-green-400 text-sm overflow-x-auto">
                <code>{directive.code}</code>
              </pre>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
